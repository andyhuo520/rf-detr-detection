# 🚀 快速开始指南

## 第一步：部署到 GitHub

### 1. 在 GitHub 创建新仓库

访问 [github.com/new](https://github.com/new) 创建新仓库：

- **Repository name**: `rf-detr-detection`
- **Description**: `基于 RF-DETR 的浏览器端实时目标检测应用`
- **Public**: ✅ 勾选（GitHub Pages 需要）
- **不要勾选** "Initialize this repository with a README"

### 2. 推送代码到 GitHub

```bash
# 进入项目目录
cd rf-detr-detection

# 关联远程仓库（替换为你的 GitHub 用户名）
git remote add origin https://github.com/你的用户名/rf-detr-detection.git

# 推送代码
git push -u origin main
```

### 3. 启用 GitHub Pages

1. 进入你的 GitHub 仓库页面
2. 点击 `Settings` → `Pages`
3. 在 `Source` 下选择 `GitHub Actions`
4. 保存设置

### 4. 等待部署完成

1. 进入仓库的 `Actions` 标签页
2. 查看工作流运行状态（约 2-3 分钟）
3. 部署成功后，访问：
   ```
   https://你的用户名.github.io/rf-detr-detection/
   ```

---

## 第二步：本地开发

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

浏览器会自动打开 `http://localhost:5173`

### 构建生产版本

```bash
npm run build
```

构建结果在 `dist/` 目录

---

## 第三步：使用应用

1. **等待模型加载**
   - 首次使用需要下载 358MB 模型
   - 模型会被浏览器缓存

2. **上传图片**
   - 点击上传区域选择图片
   - 或拖拽图片到上传区域

3. **查看检测结果**
   - 图片上会显示检测框和标签
   - 下方列表显示所有检测到的目标

---

## 常见问题

### Q: 模型加载失败？

**A:** 确保：
- 使用支持 WebGPU 的浏览器（Chrome 113+）
- 网络连接稳定
- 使用 HTTPS 访问（GitHub Pages 自动提供）

### Q: 如何更新部署？

**A:** 修改代码后：
```bash
git add .
git commit -m "描述你的更改"
git push
```

GitHub Actions 会自动重新部署。

### Q: 如何修改检测阈值？

**A:** 编辑 `main.js` 第 115 行：
```javascript
const results = await detector(img, {
    threshold: 0.5,   // 修改这个值（0-1）
    percentage: true
});
```

---

## 项目结构

```
rf-detr-detection/
├── .github/
│   └── workflows/
│       └── deploy.yml      # GitHub Actions 部署配置
├── index.html              # 主页面
├── main.js                 # 主程序逻辑
├── package.json            # 项目配置
├── vite.config.js          # Vite 配置
├── README.md               # 详细文档
├── DEPLOY.md               # 部署指南
├── LICENSE                 # 许可证
└── .gitignore              # Git 忽略文件
```

---

## 下一步

- 📖 阅读 [README.md](README.md) 了解详细功能
- 🚀 查看 [DEPLOY.md](DEPLOY.md) 了解高级部署选项
- 🎨 自定义界面样式
- 🔧 调整检测参数
- 📊 添加统计功能

---

## 需要帮助？

- 📝 [提交 Issue](../../issues)
- 📚 [查看文档](README.md)
- 🌐 [访问模型页面](https://huggingface.co/onnx-community/rfdetr_medium-ONNX)

---

**祝你使用愉快！** 🎉
